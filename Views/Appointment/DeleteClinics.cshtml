@model IEnumerable<HospitalApp.Models.Clinic>

<h1>Delete Clinics</h1>

<ul>
    @foreach (var clinic in Model)
    {
        <li>
            <anch href="/Admin/DeleteClinic/@clinic.Id">
                @clinic.Name
            </anch>
        </li>
    }
</ul>